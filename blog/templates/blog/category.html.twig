{# templates/blog/category.html.twig #}
{% extends 'base.html.twig' %}


    {% block body %}

        {% for article in articles %}

            {{ article.id }}: {{ article.content }}<br>
                {{ article.title }}<br><br>

     {% endfor %}


     <a class="btn btn-warning" href="{{ path('app_index') }}">
         Back to homepage.
     </a>
{% endblock %}