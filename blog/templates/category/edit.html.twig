{% extends 'base.html.twig' %}

{% block title %}Edit Category{% endblock %}

{% block body %}
   <H1>Edit Category </H1>

    <div class="container">

        <table class="table table-dark">

            <tbody>
            <tr>
                <td>
                    <a class="btn btn-warning" href="{{ path('category_index') }}">back to list</a>
                </td>
                <td>
                    {{ include('category/_delete_form.html.twig') }}
                </td>
            </tr>
            </tbody>
        </table>

        <table class="table table-dark">

            <tbody>
            <tr>
                <td>
                    <a>{{ include('category/_form.html.twig', {'button_label': 'Update'}) }}</a>
                </td>
            </tr>
            </tbody>
        </table>
    </div>



{% endblock %}