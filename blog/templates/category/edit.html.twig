{% extends 'base.html.twig' %}

{% block title %}Edit Category{% endblock %}

{% block body %}
    <div class="jumbotron">
        <div class="text-center"><H1>Edit Category </H1></div>
        <a href="{{ path('app_index') }}">Retour à l'accueil</a>
    </div>

    <div class="container">

        <table class="table table-dark">

            <tbody>
            <tr>
                <td>
                    <a>{{ include('category/_form.html.twig', {'button_label': 'Update'}) }}</a>
                </td>
                <td>
                    <a class="btn btn-warning" href="{{ path('category_index') }}">back to list</a>
                </td>
                <td>
                    {{ include('category/_delete_form.html.twig') }}
                </td>
            </tr>
            </tbody>
        </table>
    </div>



{% endblock %}