{% extends 'base.html.twig' %}

{% block title %}New Category{% endblock %}

{% block body %}
    <div class="jumbotron">
        <div class="text-center"><H1>Create new Category</H1></div>
        <a href="{{ path('app_index') }}">Retour à l'accueil</a>
    </div>

    <div class="container">
        <table class="table table-dark">
            <thead>
            <tr>
                <th></th>
            </tr>
            </thead>
            <tbody>
            <tr>
                <td>{{ include('category/_form.html.twig') }}</td>
            </tr>
            <td>
                <a href="{{ path('category_index') }}">back to list</a>
            </td>
            </tbody>
        </table>
    </div>

{% endblock %}