{% extends 'base.html.twig' %}

{% block title %}Edit Tag{% endblock %}

{% block body %}
    <div class="jumbotron">
        <div class="text-center"><H1>Edit Tag </H1></div>
        <a href="{{ path('app_index') }}">Retour à l'accueil</a>
    </div>

    <div class="container">

        <table class="table table-dark">

            <tbody>
            <tr>
                <td>
                    <a>{{ include('tag/_form.html.twig', {'button_label': 'Update'}) }}</a>
                </td>
                <td>
                    <a class="btn btn-warning" href="{{ path('tag_index') }}">back to list</a>
                </td>
                <td>
                    {{ include('tag/_delete_form.html.twig') }}
                </td>
            </tr>
            </tbody>
        </table>
    </div>



{% endblock %}