{% extends 'base.html.twig' %}

{% block title %}Tag{% endblock %}



{% block body %}

    <H1>Tag</H1>

    <div class="container">
        <table class="table table-dark">
            <tbody>
            <tr>
                <td>
                    <a class="btn btn-outline-warning" href="{{ path('tag_index') }}">back to list</a>
                </td>
                <td>
                    <a class="btn btn-success" href="{{ path('tag_edit', {'id': tag.id}) }}">edit</a>
                </td>
                <td>
                    {{ include('tag/_delete_form.html.twig') }}
                </td>
            </tr>
            </tbody>
        </table>
        <table class="table table-dark">
            <thead>
            <tr>
                <th>Id</th>
                <th>Name</th>
            </tr>
            </thead>
            <tbody>
            <tr>
                <td>{{ tag.id }}</td>
                <td>{{ tag.name }}</td>
            </tr>
            </tbody>
        </table>

    </div>







{% endblock %}