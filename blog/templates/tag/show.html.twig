{% extends 'base.html.twig' %}

{% block title %}Tag{% endblock %}



{% block body %}

    <div class="jumbotron">
        <div class="text-center"><H1>Tag</H1></div>
        <a href="{{ path('app_index') }}">Retour à l'accueil</a>
    </div>
    <div class="container">
        <table class="table table-dark">
            <thead>
            <tr>
                <th>Id</th>
                <th>Name</th>
            </tr>
            </thead>
            <tbody>
            <tr>
                <td>{{ tag.id }}</td>
                <td>{{ tag.name }}</td>

            </tr>
            </tbody>
        </table>

        <a class="btn btn-warning" href="{{ path('tag_index') }}">back to list</a>

        <a class="btn btn-success" href="{{ path('tag_edit', {'id': tag.id}) }}">edit</a>

        {{ include('tag/_delete_form.html.twig') }}
    </div>







{% endblock %}